# gallery

Widgets gallery

## Getting Started

Test Flutter Wasm implementation
- [Flutter Wasm](https://docs.flutter.dev/platform-integration/web/wasm)

```bash
flutter run -d chrome --wasm --release --target=lib/example_gallery.dart 
```
Issues in release mode:
 * Tooltip does not work well for the Stacked Bar Chart example (works in debug mode) 
 * Tooltip does not work well for the Grouped Bar Chart example (works in 
 debug mode)
 
